@model IEnumerable<IMS_Final_Version2.Models.dbModel.tblSector>

@{
    ViewBag.Title = "Register Book";
}

<h4>List of Register Book</h4>

<p>
    @Html.ActionLink("Create New Register Book", "Create")
</p>

<table class="table">
    <!-- <tr>
         <th>
             Sector Name
         </th>
         <th>
             Description
         </th>
         <th></th>
     </tr>-->
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.ActionLink(item.SectorName, "IndexWithSectorId", "Inventories", new { sectorId = item.SectorId }, null)
            </td>
            <!--<td>
               @* @Html.DisplayFor(modelItem => item.SectorDescription)*@
            </td>-->
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.SectorId })
            </td>
        </tr>
    }

</table>
<hr />
@Html.ActionLink("Back to Home", "defineRole", "System")

